# ─────────────────────────────────────────────────────────────
# S.Hub 통합 requirements
#   대시보드(app/) = 구 supervisor  ·  엔진(engine/) = 구 orchestration
# ─────────────────────────────────────────────────────────────

# ══ 대시보드 (구 supervisor/backend) ══
# 실제 설치 버전으로 핀 (pip freeze 기준 직접 의존성 고정)
fastapi==0.136.3
uvicorn[standard]==0.48.0
python-multipart==0.0.32
# TODO 버전 미확정: engine 실행 전 검증 필요 (orch=1.5.2)
yfinance==1.4.1
# TODO 버전 미확정: engine 실행 전 검증 필요 (orch=2.3.3)
pandas==3.0.3
# TODO 버전 미확정: engine 실행 전 검증 필요 (orch=2.2.6)
numpy==2.4.6
python-dotenv==1.2.2
azure-cognitiveservices-speech==1.50.0
# RAG
# TODO 버전 상향: 통합 전 대시보드는 2.38.0 이었으나 engine 의 langchain-openai==1.4.1 이
#   openai>=2.45.0 을 요구해 해소 불가였다. 두 제약을 만족하는 최소 버전으로 올린다.
openai==2.45.0
# TODO 버전 미확정: engine 실행 전 검증 필요 (orch=6.14.2)
pypdf==6.15.0
tiktoken==0.13.0  # ingest_documents.py 청킹(cl100k_base) — openai 간접의존 아님, 직접 핀
# DART 회사코드 적재(scripts/ingest_corp_code.py) — httpx 는 supabase/openai 간접의존이라
# 깔려는 있으나 직접 핀이 없어(tiktoken 과 같은 함정) 설치 버전으로 명시 핀한다.
httpx==0.28.1
# JWT 검증(app/core/auth.py) — supabase 간접의존이라 깔려는 있으나 직접 핀이 없었다.
# 비대칭(ES256) JWKS 검증에 cryptography 가 필요하므로 PyJWT[crypto] 로 직접 핀한다.
# supabase-auth 가 pyjwt>=2.12.0 을 요구하므로 그 범위 안의 설치 버전으로 핀.
PyJWT[crypto]==2.13.0
# DB (Supabase 연동: A=supabase-py RPC 전용 / B=psycopg3 직접 SQL)
psycopg[binary]==3.3.4
supabase==2.31.0

# ══ 엔진 (구 orchestration) ══
# LangStack pins are Dependabot-managed releases from public PyPI.
# CI installs this file on a clean Python 3.10 runner as the availability gate.
langgraph==1.2.10
langchain==1.3.14
langchain-openai==1.4.1
langchain-chroma==1.1.0
langsmith==0.10.14
chromadb==1.5.9
pydantic==2.12.3
# (대시보드 섹션에서 이미 선언: numpy==2.4.6)  ← 충돌, orch=numpy==2.2.6
# (대시보드 섹션에서 이미 선언: pandas==3.0.3)  ← 충돌, orch=pandas==2.3.3
pyarrow==23.0.1
scipy==1.15.3
PyYAML==6.0.3
# (대시보드 섹션에서 이미 선언: yfinance==1.4.1)  ← 충돌, orch=yfinance==1.5.2
# (대시보드 섹션에서 이미 선언: python-dotenv==1.2.2)
tomli==2.4.1; python_version < "3.11"
# (대시보드 섹션에서 이미 선언: pypdf==6.15.0)  ← 충돌, orch=pypdf==6.14.2
pytest==9.1.1
streamlit==1.60.0
