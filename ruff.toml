# lint 결과를 버전에 관계없이 재현 가능하게 고정한다.
# ruff 0.16.0에서 기본 룰셋이 확장되면서(I001·BLE001·RUF100 등) 코드 변경 없이
# CI만 실패했기 때문에, 적용 규칙을 레포에서 명시한다.
# 규칙을 넓히는 변경은 코드 수정과 함께 별도 PR에서 논의한다.
target-version = "py310"

[lint]
# ruff 0.15까지의 기본값(E4·E7·E9·F)을 그대로 명시한 것이다.
select = ["E4", "E7", "E9", "F"]
