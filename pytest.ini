# 통합 레포의 테스트 경로 설정.
#
# 엔진(engine·scripts/engine·tests/engine)과 콘솔(console)은 리포 루트의 최상위
# 패키지이고, 대시보드 코드는 backend/ 아래에 있다. 두 import 루트를 모두 명시한다.
#   .        → engine · console · scripts.engine · tests.engine
#   backend  → app (대시보드)
[pytest]
pythonpath = . backend
