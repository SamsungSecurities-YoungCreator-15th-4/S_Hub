# 통합 레포의 테스트 경로 설정.
#
# 엔진(backend/engine·backend/scripts/engine)과 콘솔(console/)은 원래 별도 레포의
# 최상위 패키지였다. 통합하면서 위치가 갈렸으므로 import 루트를 명시한다.
#   .        → console
#   backend  → app(대시보드) · engine · scripts.engine · tests.engine
[pytest]
pythonpath = . backend
